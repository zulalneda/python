import numpy as np
import pandas as pd

##concat = birleştirme pd.concat([dataFrame1,dataFrame2,dataFrame3]) yan yana birleştirir.


##concat = birleştirme pd.concat([dataFrame1,dataFrame2,dataFrame3],axis = 1) dikey şekilde birleştirir. (alt alta)

# pd.merge(mergeDateFrame1,mergeDateFrame2,on = "Isim")
## merge = birleştirmek, kaynaştırmak. İlk iki parametredeki değerleri birleştirmek suretiyle 3. parametreye "on=" yazarak ortak noktasını yazıyor.
